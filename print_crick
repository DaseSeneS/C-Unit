using UnityEngine;

public class Crickets : MonoBehaviour
{
    // Этот метод вызывается каждый кадр
    void Update()
    {
        // Выводим слово "crickets" в консоль
        Debug.Log("crickets");
    }
}
